<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.dynamicbatch</groupId>
        <artifactId>dynamic-batch</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>dynamic-batch-benchmark</artifactId>
    <name>dynamic-batch-benchmark</name>
    <description>单分区权威压测（跑 SinglePartitionPerf.main；不进 CI）</description>

    <dependencies>
        <dependency>
            <groupId>com.dynamicbatch</groupId>
            <artifactId>dynamic-batch-core</artifactId>
        </dependency>
        <!-- 唯一日志绑定：core 的 mail starter 是 optional 不传递，这里显式声明，
             避免压测时出现 slf4j 多绑定（版本由根 pom 导入的 spring-boot-dependencies 管理） -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
    </dependencies>
</project>
